export default function AdminDashboard() {
  return (
    <div className="rounded-2xl border bg-white p-5 shadow-sm">
      <h1 className="text-lg font-semibold">Admin Dashboard (Next step)</h1>
      <p className="mt-2 text-sm text-gray-600">
        Next we’ll build package CRUD here (admin/packages endpoints).
        Admin password reset is intentionally not shown.
      </p>
    </div>
  );
}